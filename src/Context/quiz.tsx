/*import React, { ReactNode, createContext, useReducer } from "react";
//import questions  from "../perguntas/questions";

/*const STAGES =["Start", "Playing", "End"]

const initialState = {
  gameStage: STAGES[0],
  questions
}

const quizReducer = (state, action) =>{
  console.log(state, action);
  switch(action.type){
    case "CHANGE_STATE":
      return state;
    
      default:
        return state;
  }
}

interface QuizContextType {
  // QuizContextType define a estrutura do contexto
  name: string;
}

const defaultValue: QuizContextType = {
  name: "Quiz",
};

export const QuizContext = createContext(defaultValue);

export function QuizProvider({ children }: { children: ReactNode }) {
  return (
    <QuizContext.Provider value={defaultValue}>{children}</QuizContext.Provider>
  );
}*/
